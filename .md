this for the seo structure <!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#f6f6f6" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <!--
      Google Search Console verification.
      Paste the content value from search.google.com/search-console here.
      Get the code by adding a "URL prefix" property, then choosing the
      "HTML tag" verification method. It looks like:
        <meta name="google-site-verification" content="AbC-123xyz..." />
      Leave the placeholder below until you have it — it's harmless.
    -->
    <meta name="google-site-verification" content="REPLACE_WITH_YOUR_CODE" />

    <!--
      Default tags for the dev server and as a fallback.
      On `npm run build`, scripts/prerender-seo.js replaces the
      <title>, description, and OG/Twitter tags in each
      dist/<route>/index.html with that page's specific values.
      Do not remove the <title> or description — they serve as
      the dev-mode defaults and prevent FOUC (flash of unstyled content)
      on very first paint before the router mounts.
    -->
    <title>The Cloud Group | Custom Software That Ships On Time</title>
    <meta
      name="description"
      content="We build custom software that ships on time — or you don't pay. 15+ years, 2,000+ projects, 9 countries."
    />

    <!--
      Open Graph defaults — used by the dev server. On build, every
      route gets its own tags via scripts/prerender-seo.js.
    -->
    <meta property="og:site_name" content="The Cloud Group" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content="The Cloud Group | Custom Software That Ships On Time" />
    <meta property="og:description" content="We build custom software that ships on time — or you don't pay." />
    <meta property="og:image" content="/og/favicon.svg" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content="en_US" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="The Cloud Group | Custom Software That Ships On Time" />
    <meta name="twitter:description" content="We build custom software that ships on time — or you don't pay." />
    <meta name="twitter:image" content="/og/favicon.svg" />
  </head>
  <body>
    <!--
      React mounts here. On production build, the prerender-seo script
      injects a fallback <header>, <main>, <h1>, and nav links INSIDE
      this div so crawlers see real content. React's createRoot() wipes
      it on mount, so users never see it.
    -->
    <div id="root"></div>

    <!--
      Fallback for browsers with JavaScript disabled (and some crawlers).
      Shown only when JS is off. React replaces the app once it mounts.
    -->
    <noscript>
      <div style="padding: 24px; font-family: system-ui, sans-serif; max-width: 720px; margin: 0 auto;">
        <h1>The Cloud Group</h1>
        <p>
          We build custom software that ships on time — or you don't pay.
          15+ years. 2,000+ projects. 9 countries.
        </p>
        <p>
          JavaScript is disabled in your browser. To view the full site,
          please enable JavaScript or contact us directly:
        </p>
        <ul>
          <li><a href="tel:+34660014818">+34 660 014 818</a></li>
          <li>
            <a href="https://wa.me/34660014818" rel="noreferrer">
              Message us on WhatsApp
            </a>
          </li>
        </ul>
      </div>
    </noscript>

    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>




